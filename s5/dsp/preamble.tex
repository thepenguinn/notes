% height = 5.9in width = 4.37in
% make this variable
%% \usepackage[paperheight = 4.37in, paperwidth = 5.9in, margin=0.2in]{geometry}
\usepackage[a4paper, margin=1in]{geometry}

\usepackage{adjustbox}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{circuitikz}
\usepackage{subfiles}
\usepackage{amsmath, amssymb}
\usepackage{enumitem}
\usepackage{nicematrix}
\usepackage{minted}
\usepackage{caption}
\usepackage{lmodern}
\usepackage{bookmark}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{multicol}
\usepackage{booktabs}
\usepackage{titlesec}

\usepackage[bottom]{footmisc}

\usepackage{setspace}
%\usepackage[skip=15pt plus1pt]{parskip}

\usepackage{varwidth}
\usepackage{titletoc}
\usepackage{ifthen}
\usepackage{gensymb}
\usepackage{siunitx}
\usepackage{pgfplots}

%% custom packages
\usepackage{colorscheme}
\usepackage{cornercommands}

\pgfplotsset{
    compat=newest,
    colormap={black}{rgb255=(0,0,0) rgb255=(0,0,0)},
    FrontViewStyle/.style={
        view = {90}{0},
        height = 3.13in,
        width = 3.13in,
        %%
        xlabel = Time,
        %%
        ylabel = Real Part,
        ymin = -1.25,
        ymax = 1.25,
        %%
        zlabel = Imaginary Part,
        zmin = -1.25,
        zmax = 1.25,
        %%
        grid = both,
        xticklabel style = {font = \small},
        yticklabel style = {font = \small},
        zticklabel style = {font = \small},
        %%
        xlabel style = {font = \small},
        ylabel style = {font = \small},
        zlabel style = {font = \small},
    },
    PlotComplex3DStyle/.style={
        height = 3.92in,
        width = 6.27in,
        plot box ratio = 4 1.5 1,
        %%
        xlabel = Time,
        xmin = -75,
        xmax = 2575,
        %%
        ylabel = Real Part,
        ymin = -1.25,
        ymax = 1.25,
        %%
        zlabel = Imaginary Part,
        zmin = -1.25,
        zmax = 1.25,
        %%
        xtick = {0, 312.5, ..., 2500},
        grid = both,
        xticklabel style = {font = \small},
        yticklabel style = {font = \small},
        zticklabel style = {font = \small},
        xticklabels = {$0$, $\dfrac{\pi}{32}$, $\dfrac{\pi}{16}$, $\dfrac{3 \pi}{32}$, $\dfrac{\pi}{8}$, $\dfrac{5 \pi}{32}$, $\dfrac{3 \pi}{16}$, $\dfrac{7 \pi}{32}$, $\dfrac{\pi}{4}$, },
    },
    PlotShortXStyle/.style = {
        height = 1.76in,
        width = 3.13in,
        xlabel = Time,
        ylabel = Amplitude,
        xtick = {0, 156.25, ..., 625},
        grid = both,
        xticklabel style = {font = \small},
        yticklabel style = {font = \small},
        xticklabels = {$0$, $\dfrac{\pi}{64}$, $\dfrac{\pi}{32}$, $\dfrac{3 \pi}{64}$, $\dfrac{\pi}{16}$, },
    },
}

\usetikzlibrary{intersections}
\usetikzlibrary{positioning}
\usetikzlibrary{calc}
%% \usetikzlibrary{external}
\usetikzlibrary{ext.topaths.arcthrough}

\usepgfplotslibrary{groupplots}

\pgfplotsset{every tick label/.append style={font=\small}}

%\onehalfspacing

\setlength{\arraycolsep}{0pt}
\renewcommand\arraystretch{2}

%% \makeatletter
%% \def\maxwidth{\ifdim\Gin@nat@width>0.8\linewidth0.8\linewidth\else\Gin@nat@width\fi}
%% \def\maxheight{\ifdim\Gin@nat@height>0.9\textheight0.9\textheight\else\Gin@nat@height\fi}
%% \makeatother
%% % Scale images if necessary, so that they will not overflow the page
%% % margins by default, and it is still possible to overwrite the defaults
%% % using explicit options in \includegraphics[width, height, ...]{}
%% \setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
%% % Set default figure placement to htbp

\makeatletter
\def\fps@figure{htbp}
\makeatother
