\begin{tikzpicture} [
        transform shape,
        rounded corners = 0.3cm,
        every path/.style = {thick, > = latex},
        % scale = 4,
    ]

    \node[block]
    (transmitter) {Transmitter};
    \node[block, below=of transmitter, xshift = 2cm, yshift = -1cm]
    (channel) {Channel};
    \node[block, above=of channel, xshift = 2cm, yshift = 1cm]
    (receiver) {Receiver};

    \draw[->] (transmitter.south) |- (channel.west)
    node[label, midway, left = -0.25cm]
    {Transmitted \\ signal};

    \draw[->] (channel.east) -| (receiver.south)
    node[label, midway, right = -0.25cm]
    {Received \\ signal};

    \node[block, left=of transmitter, yshift = -2cm]
    (source)
    {Source of \\ information};

    \node[block, right=of receiver, yshift = -2cm]
    (user) {User of \\ information};

    \draw[->] (source.north) |- (transmitter.west)
    node[label, midway, above = 0.5cm]
    {Message \\ signal};

    \draw[->] (receiver.east) -| (user.north)
    node[label, midway, above = 0.5cm]
    {Estimate of \\ message signal};


    \node[label, above = of channel, yshift = 3cm]
    {\textsc{Communication System}};

    \node[block, below = of channel, yshift = -0.5cm] (noise)
    {Noise};

    \draw[->] (noise.north) -- (channel.south);

\end{tikzpicture}
